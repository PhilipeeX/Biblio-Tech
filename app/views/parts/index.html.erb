<p style="color: green"><%= notice %></p>

<h1><%= I18n.t('supplier.part.view_index_title') %></h1>

<div id="parts">
  <% @parts.each do |part| %>
    <%= render part %>
    <p>
      <%= link_to I18n.t('supplier.part.show'), part %>
    </p>
    <hr>
  <% end %>
</div>

<%= link_to I18n.t('supplier.part.new'), new_part_path %> |