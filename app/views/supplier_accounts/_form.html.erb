<%= form_with(model: supplier_account) do |form| %>
  <% if supplier_account.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(supplier_account.errors.count, "error") %> prohibited this supplier_account from being saved:</h2>

      <ul>
        <% supplier_account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :account, style: "display: block" %>
    <%= form.number_field :account %>
  </div>

  <div>
    <%= form.label :supplier_id, style: "display: block" %>
    <%= form.text_field :supplier_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
