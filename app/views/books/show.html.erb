<p style="color: green"><%= notice %></p>

<%= render @book %>

<div>
  <%= link_to I18n.t('author.book.edit'), edit_book_path(@book) %> |
  <%= link_to I18n.t('author.book.back'), books_path(@book) %>

  <%= button_to I18n.t('author.book.destroy'), book_path(@book), method: :delete %>
</div>
