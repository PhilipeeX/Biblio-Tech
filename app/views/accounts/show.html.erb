<p style="color: green"><%= notice %></p>

<%= render @account %>

<div>
  <%= link_to I18n.t('account.edit'), edit_supplier_account_path(@account.supplier_id, @account) %> |
  <%= link_to I18n.t('account.back_supplier'), supplier_path(@supplier) %>

  <%= button_to I18n.t('account.destroy'), supplier_account_path, method: :delete %>
</div>
